Operator Packet: COMPLETE

# Hero 1 DOT Production Binding — Phase 1 Operator Packet

**Task:** Make the Hero 1 DOT weapon production Combat VFX binding real and active, matching the AOE/Pierce/Bounce production binding method class.
**User request (verbatim):** "Ok can we have the DOT weapon production row be active like the others? There is no reason it should not be."
**Approval source:** `Reports/AgentReviews/Hero1DOTProductionBinding/codex_operator_approval_phase1.md` (Phase 1, FullOperator, approved scope only).
**Date:** 2026-05-30.

## Result

The DOT weapon production row `Hero1Axe_DOT_Base` is now **active** like AOE/Pierce/Bounce. It is backed by a **real Niagara mesh-renderer carrier** generated under `Content/VFX/Hero1/Axe/DOT/`, and `PerformDOT` resolves that binding and attaches the authored carrier to the single moving hero→target DOT shot (runtime only transports it). No fake CSV-only row: the shot silhouette is authored by the Niagara/material/mesh asset, not by actor-side temporary mesh geometry.

## Critical-guard compliance

- **Real carrier exists:** `NS_Hero1AxeDOT_MeshSlash` + `SM_Hero1AxeDOT_AuraRing` generated by the `T66Hero1AxeDOTVFX` commandlet (C++) into `/Game/VFX/Hero1/Axe/DOT/`, not hand-faked.
- **Runtime uses the carrier:** `PerformDOT` calls `ResolveCombatVFXBinding(WeaponBase, ResolveWeaponImpactSourceID(), AttackCategory, ...)` and passes the resolved Niagara system into `SpawnVisualTravelProjectile`, which `SpawnSystemAttached`s the carrier to the projectile root and `HideMesh`es the temporary profile meshes (same seam class as Bounce).
- **Silhouette authored by Niagara/material/renderer:** carrier uses 3 mesh-renderer emitters on `SM_Hero1AxeDOT_AuraRing` with shared Hero 1 slash materials; temporary profile meshes are hidden when the carrier resolves.

## Behavior preservation (DOT damage/timing unchanged)

- Initial contact damage stays **synchronous** in `PerformDOT`.
- Weapon impact-context publication stays **synchronous** at the official target aim/impact point.
- Exactly **one** `ApplyDOT(... SourceID=HeroPrimaryDot)` payload — the three sphere markers remain visual applicators, not damage lanes.
- Marker reveal cadence (0.00 / 0.50 / 1.00s) and 0.30s visible-pulse duration **unchanged**.
- DOT tuning (duration, tick interval, per-tick damage) **unchanged**.
- No idol production rows added; no Mini/minigame scope touched.
- Dev-fallback preserved: if the binding fails to resolve, the temporary DOT profile is still the mover so DOT ticking and bring-up are never lost.

## Changed files

Source (runtime):
- `Source/T66/Gameplay/T66CombatComponent.h` — `SpawnVisualTravelProjectile` gains optional `UNiagaraSystem* CarrierSystem = nullptr`, `float CarrierVisualScale = 1.f`.
- `Source/T66/Gameplay/T66CombatComponent.cpp` — (a) carrier-attach branch in `SpawnVisualTravelProjectile` (`SpawnSystemAttached` + `HideMesh` on `VisualMesh`/`AccentMesh` when a carrier is supplied; else fall back to `ConfigureTemporaryProjectileVisual`); (b) `PerformDOT` resolves the `Hero1Axe_DOT_Base` binding and feeds the carrier system + visual scale into the DOT shot; `T66DotShotSpawned` log extended with `Carrier=... CarrierVisualScale=...`.

Data / scripts:
- `Content/Data/CombatVFXBindings.csv` — active `Hero1Axe_DOT_Base` row (`Hero_1_black_dot`, `DOT`, Niagara `/Game/VFX/Hero1/Axe/DOT/NS_Hero1AxeDOT_MeshSlash`, `MeshSlashDOT`, `bSuppressTemporaryProjectile=True`, BaseVisualRadius 80, VisualScaleMultiplier 1.0).
- `Content/Data/DT_CombatVFXBindings.uasset` — re-imported from CSV (re-saved 06:21).
- `Scripts/SetupCombatVFXBindingsDataTable.py` — `HERO1_AXE_DOT_BINDING_ROW` added to `ENFORCED_BINDING_ROWS`.
- `Scripts/ValidateCombatVFXProductionBindings.py` — added `PRODUCTION_DOT_NIAGARA` + `PRODUCTION_DOT_RING_MESH` to `REQUIRED_ASSETS`; added `validate_dot_csv_binding()` and wired it into `run_unreal_validation`.

Docs:
- `Gameplay/Combat/VFX_PROCESS_INDEX.md`, `Gameplay/Combat/Hero1AxeVFXPlan.md`, `Gameplay/Combat/CombatVFXInfrastructureInventory.md`, `Gameplay/Combat/Hero1AxeDOTMechanismPacket.md` — DOT updated to "active production binding row"; final visual-polish / Pablo sign-off disclaimer preserved.

## Generated assets

| Asset | Path | Size (bytes) |
|---|---|---|
| DOT carrier Niagara system | `/Game/VFX/Hero1/Axe/DOT/NS_Hero1AxeDOT_MeshSlash.uasset` | 648,758 |
| DOT aura-ring mesh | `/Game/VFX/Hero1/Axe/DOT/SM_Hero1AxeDOT_AuraRing.uasset` | 44,318 |

Commandlet log (UE): ring mesh `vertices=3456 indices=3456 sections=1`; 3 ring mesh renderers bound to `SM_Hero1AxeDOT_AuraRing` using shared materials `M_Hero1AxeAOE_Slash_Bright/Body/Dark`; `ProductionPaths=true`.

## Commands and outcomes

| Step | Command | Outcome |
|---|---|---|
| Compile | `Build.bat T66Editor Win64 Development -Project=...T66.uproject -WaitMutex -NoHotReloadFromIDE` | **Succeeded** (exit 0, 97.67s; `DOTBindingPhase1_Build.log` line 37 "Result: Succeeded"). Pre-existing C4996 Niagara `IsReadyToRun` deprecation warning only; not introduced here. |
| Asset gen | `UnrealEditor-Cmd ...T66.uproject -run=T66Hero1AxeDOTVFX -T66Hero1AxeDOTProduction -unattended -nop4 -nosplash` | **exit 0**; both production assets saved to `/Game/VFX/Hero1/Axe/DOT/`. |
| DataTable reload | `UnrealEditor-Cmd ...T66.uproject -run=pythonscript -script=Scripts/SetupCombatVFXBindingsDataTable.py -unattended -nop4 -nosplash` | **exit 0**; `DT_CombatVFXBindings.uasset` re-saved 06:21. |
| Validation | `UnrealEditor-Cmd ...T66.uproject -run=pythonscript -script=Scripts/ValidateCombatVFXProductionBindings.py -unattended -nop4 -nosplash` | **exit 0**; "Combat VFX production binding validation DONE". |

Validator confirmations (UE LogPython, `[CombatVFXProductionBindingsValidate]`):
- `Active binding rows: ['Hero1Axe_AOE_Base', 'Hero1Axe_Pierce_Base', 'Hero1Axe_Bounce_Base', 'Hero1Axe_DOT_Base']; deferred scaffold rows: []`
- `DOT CSV binding row is production-bound to the moving aura-ring carrier Niagara system and suppresses the temporary projectile`
- `Required production DataTable/Niagara/material/mesh/texture assets exist`
- `Production VFX assets do not depend on /Game/VFXLab`
- `Source guards for binding dispatch, item scaling, and production proof mode are present`
- `BP_T66GameInstance points at DT_CombatVFXBindings`

## Caveats / skipped verification

- **No visual sign-off.** This packet proves binding structure, required assets, source guards, and data contracts only. It does **not** prove visual fidelity, temporal mechanism quality, final player-facing readability, or Pablo visual approval. The sphere applicator markers remain intentional placeholders, and the aura-ring carrier's final visual polish is deferred.
- **No gameplay capture run** in this phase — that is the Phase 2 runtime proof (below).
- **No git operations** performed (no add/commit/push/tag/reset/clean) per approval scope; working tree left staged for review.
- The reload step's standalone `-log` file did not materialize (absolute `-log` path not honored by the python commandlet invocation); reload effect is instead evidenced by the `DT_CombatVFXBindings.uasset` re-save at 06:21 and the subsequent validator reading the correct active-row end-state.

## Next required Phase 2 runtime proof

Gameplay capture proving one moving DOT shot carrying the authored aura-ring carrier + three sequentially-revealed sphere markers + single `HeroPrimaryDot` payload:

```
Scripts/CaptureT66GameplayVideo.ps1 -CaptureMode hero1axedotvfxbinding -UseHero1AxePreviewStaging -NoHero1AxeTargets -FrameCount 120 -EvidenceBundle -EvidenceAutoSelectFrames
```

Evidence to confirm in that capture: `T66DotShotSpawned ... Carrier=/Game/VFX/Hero1/Axe/DOT/NS_Hero1AxeDOT_MeshSlash...`; three `T66DotApplicatorMarkerRevealed` at planned delays 0.00/0.50/1.00s each followed by `T66DotApplicatorMarkerHidden ... VisibleDurationSeconds=0.30`; and DOT tick damage flowing through the single `HeroPrimaryDot` source rather than three multiplied lanes.

## Claude token usage

Not exposed to the agent in this environment; unavailable.
