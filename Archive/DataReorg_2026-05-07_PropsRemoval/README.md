# Deprecated world props archive

The live game no longer uses the generated world prop assets under `/Game/World/Props`.

The previous `Content/Data/Props.csv` contents are preserved here as reference only so
the old placement data is not lost silently. Do not reload this file into
`/Game/Data/DT_Props` unless the props feature is intentionally restored.

