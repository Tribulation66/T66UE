# Current Main Menu Reference

This folder contains the current authoritative visual target for the
FriendslopStyle Main Menu pilot.

Reference image:

`main_menu_reference_02_rubber_statue_title_fixed_cli.png`

Archived previous current reference:

`UI/FriendslopStyle/Archive/ReferenceIterations/MainMenu/Round07/`

The image is a visual target and comparison artifact only. It must not be
pasted into runtime UI and must not be cropped into runtime chrome, title, text,
icon, or data assets.

The current manifest and prompt document how this reference was generated and
approved for the next implementation iteration.
