Rejected Pass 01: color/material family is usable, but atomic gate failed. The sheet includes arrow symbols baked into scrollbar arrow buttons and a composite table strip with column dividers attached to a header-like dark bar, which would force a mini-layout into runtime assets. A large panel also contains a non-reference cutout shape. Regenerate with stricter standalone empty components.
