# Deprecated UI Reference Cleanup - 2026-04-25

These screen packs were moved out of the active UI reference workspace because they are deprecated or stale generation targets for the current UI image-generation process.

Do not use these folders as active generation tasks or style references. Preserve them only as historical/code-evidence artifacts for source cleanup or audit work.

Archived from `UI\screens`:
- `wheel_overlay`
- `party_size_picker`
- `casino_overlay`
- `gambler_overlay`
- `vendor_overlay`
- `leaderboard`

Canonical active naming now uses:
- `powerup` instead of `shop`
- `minigames` instead of `unlocks`
