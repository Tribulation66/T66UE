Use case: screen-specific T66 UI style reference gate
Canvas: 1920x1080 exactly
Target: # Crate Overlay Layout List

You are generating the canonical offline reference image for this exact in-game HUD or in-run overlay target. Use exactly these three inputs and no other visual references:
1. Attached image 1: the approved main-menu canonical style anchor. Extract only the visual language: chunky fantasy stone, dark dungeon panels, dark wood, beveled frames, green/gold/blue/purple button families, readable cream/gold text treatment.
2. Attached image 2: the current packaged runtime screenshot for this target. Preserve its layout intent, anchors, hierarchy, region count, proportions, visible controls, and live-data wells.
3. The layout list below. Treat it as the ownership contract for generated shell art versus runtime-owned live content.

Generate one full-screen target reference. This reference is not a runtime overlay and must not bake live/localizable data into art. Keep all runtime content sockets open: portraits, item icons, ability icons, idol icons, minimap/map pixels, markers, item names, gold/debt values, timers, cooldowns, damage numbers, selected idol data, tab labels, and status text must remain visually represented as live wells or editable/runtime text zones, not painted-in final data.

Preserve behavior and layout intent. Do not redesign the screen, move major panels, add unrelated features, change gameplay state, or obscure current controls. Restyle plain/flat panels, frames, slots, tabs, buttons, and overlay shells into the approved Chadpocalypse stone/dungeon UI family. Separate shell/frame art from runtime-owned interiors. Use dark dungeon surfaces, beveled stone borders, carved wood accents, metal rivets where helpful, and readable cream/gold text zones.

Avoid: full-screen reference-image-as-runtime-overlay look, baked item names or values, changed tab labels, illegible text wells, extra nonfunctional buttons, missing controls, missing sockets, changed minimap/map ownership, layout drift, watermark, UI covering gameplay-critical live regions.

Layout list and ownership contract:

# Crate Overlay Layout List

Reference gate target: CS-style crate opening overlay presentation.

## Regions

- `crate_overlay_shell` - centered presentation frame; ownership: generated-shell.
- `strip_container_frame` - item scrolling strip frame; ownership: generated-shell.
- `item_tile_sockets` - repeated item tiles inside strip; ownership: socket-frame with runtime item icons/rarity.
- `winner_marker` - center pointer/selection marker; ownership: generated-shell/runtime-state.
- `skip_hint_text` - skip instruction/status; ownership: runtime-text.
- `result_status_area` - resolved item/reward message if visible; ownership: runtime-text/runtime-value.

## Controls And States

- `crate_overlay_frame` - required states: opening, scrolling, resolved.
- `item_tile` - required states: normal, highlighted/winner, rarity variants.
- `skip_control` - required states: available, disabled, hidden.

## Live Runtime Content

- Live text: skip hint, resolved item name/status.
- Live values: winner rarity, scroll offset, draw result.
- Live image/icon/avatar wells: runtime item icons and rarity colors.
- Live preview/media areas: scrolling strip animation.

## Variants

- Pre-scroll opening.
- Active scrolling.
- Resolved item state.

