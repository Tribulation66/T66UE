# Operator Phase 0 Findings — Deprecated Code Cleanup

- **Operator:** Claude (`claude-opus-4-8`, FullOperator)
- **Validator:** Codex
- **Date:** 2026-05-29
- **State:** Read-only Phase 0 complete. **No edits, deletes, builds, stages, captures, or git operations performed.**
- **Purpose:** Lock fresh anchors for every cleanup target and surface divergences from `operator_plan_packet.md` that require a Codex ruling before any edit.

All paths relative to `C:\UE\T66`. Line numbers current as of this pass.

---

## Summary of dispositions

| Phase | Target | Disposition |
|---|---|---|
| 1 | Rich-basic routing remnants | Verified; footprint entangled with route-attribution diagnostics — see Divergence A |
| 2 | 5 inert route/touch CVars | Confirmed **inert**; footprint spans 3 files incl. perf/smoke harness — **DECISION NEEDED (Divergence A)** |
| 3 | `AT66BossProjectile` | Scan **clean** — safe to delete (see below) |
| 4 | GamblerToken legacy | In scope; footprint **much larger** than plan listed — see Divergence B |
| 5 | CoreRedirects | Scan **clean** for all 5 old-names — safe to remove all 5 |
| 6 | `C:\UE\T66_B13_Worktree` | **UNEXPECTED STATE** — not a registered worktree, no `.git` — **STOP (Divergence C)** |

---

## Phase 3 — `AT66BossProjectile`: clean, safe to delete

Fresh static + binary scan:
- **No** `SpawnActor`/`SpawnActorDeferred<AT66BossProjectile>` site anywhere in `Source/`.
- The only `AT66BossProjectile` references: the class definition (`T66BossProjectile.{h,cpp}`), the include + cleanup-filter line at `T66GameMode_Backrooms.cpp:10,151`.
- The `T66BossProjectile*` substring hits in `T66ProjectileManagerSubsystem.{h,cpp}` are the unrelated struct `FT66BossProjectileVisualKey` (boss VFX bucket key) — **not** the actor class.
- The `T66BossProjectileSmoke=`/`...SmokeComplete` hits in `T66PlayerController_Overlays.cpp:4522-4708` are CLI-flag strings + an exit-status tag; that smoke path drives bosses through `UT66ProjectileManagerSubsystem`, **not** the deprecated actor (`:4527`, `:4704-4706`).
- **Binary scan:** `rg -a -l "AT66BossProjectile"` across `Content/` → **no matches** (no Blueprint/subclass refs).

**Conclusion:** `AT66BossProjectile` is dead. Safe to delete `T66BossProjectile.{h,cpp}` and remove the include + filter line at `T66GameMode_Backrooms.cpp:10,151`. Retain the `AT66EnemyProjectileBase` filter line at `:152` (load-bearing — see below).

## `AT66EnemyProjectileBase`: confirmed load-bearing — RETAIN

Live references confirmed this pass: subclass `AT66SpitProjectile` (`T66SpitProjectile.h:10`), live `TSubclassOf<AT66EnemyProjectileBase> ProjectileClass` in `T66MobBase.h:154` and `T66RangedEnemy.h:40`, and `GetActiveEnemyProjectileCount()` consumed by `T66PerformanceSubsystem.cpp:1342` and `T66LagTrackerSubsystem.cpp:371`. Retained as instructed.

## Phase 5 — CoreRedirects: all 5 old-names clean

Text + binary scan for the 5 redirect old-names:
- `Source/` → **no matches** for any of: `GameplayFloorsPerStage`, `InitialEnemiesPerGameplayFloor`, `InitialTowerEnemiesPerGameplayFloor`, `ApplyGamblersTokenPickup`, `GetActiveGamblersTokenLevel`.
- `Config/` → only the redirect lines themselves (`DefaultEngine.ini:16-21`).
- `Content/Data/DT_PlayerExperience.uasset` → **no matches** for the 3 floor old-names (`rg -a -c`, exit 1). The "Phase 2 uasset rebuild" the redirect header bridges appears already done.
- `Content/` (all) → **no matches** for `AT66BossProjectile`, `ApplyGamblersTokenPickup`, `GetActiveGamblersTokenLevel` (no Blueprint references to the renamed UFUNCTIONs).
- Localization archives (`Content/Localization/T66/*/T66.archive`, manifest) match only the loc **key** string `GamblerToken` (3× in en.archive) — generated loc data referencing the NSLOCTEXT key, **not** a code/redirect reference; regenerated by the loc gather. Not a blocker.

**Conclusion:** All 5 redirects (`:16-18` property, `:20-21` function) are clean by text + binary scan → removable per the gate rule. (Pending the Divergence rulings, since this is part of the same pass.)

---

## DIVERGENCE A (Phase 1 + 2) — DECISION NEEDED: CVar footprint spans the perf/smoke harness

**Plan packet said:** the 5 CVars are defined/read in `T66MobManagerSubsystem.cpp` (`:60-69`, `:2232-2235`); Phase 2 file scope = that one file.

**Actual current state:**
- **4 of the 5 CVars are defined in `T66EnemyDirector.cpp:51-97`**, not the manager:
  `T66.Mob.UseLightweight` (`:51`), `RouteRushLightweight` (`:63`), `RouteFlyingLightweight` (`:75`), `RouteRangedLightweight` (`:87`).
- The 5th, `T66.Mob.Diagnostics.UseTouchDamageOverlap`, is in `T66MobManagerSubsystem.cpp:60`.
- **Inert confirmed:** the director never reads the 4 CVar *variables* — routing uses `T66IsLightweightMobRoutingEnabled()` (`:99-102`, returns `true`), `ShouldRouteSpawnToLightweightMob` voids the routing flag (`(void)bUseLightweightRouting;` `:717`), and the `T66ShouldRoute*ToLightweight()` helpers (`:104-117`) all return `true` and are `[[maybe_unused]]`. The touch-overlap CVar is `(void)`-ed in `ShouldUseTouchDamageOverlap()` (`:116-120`); its caller at `:737` consumes a constant. **No CVar gates production behavior.**
- **BUT** all 5 CVar *names* are heavily referenced as test knobs in the perf/smoke harness `T66PlayerController_Overlays.cpp`: smoke modes set them via `FindConsoleVariable` (`:1737,1894,1911,2114,2336,2341,2665,2853,2873`), command-line overrides parse + set them (`:2876-2912`: `T66MobUseTouchDamageOverlap=`, `T66MobRouteRushLightweight=`, etc.), and the `MobRoutingFlags` summary log reads all 5 (`:2958-2963`). These reads/sets are themselves inert for production, but they are live diagnostic plumbing in the instrument used for the required `enemywaveperf` verification.
- The 4 route CVars are also read as `RouteAttributionSummary` log fields in `T66MobManagerSubsystem.cpp:2232-2235` (diagnostic only).

**Why this is a stop:** removing the 5 CVars cleanly touches **3 files (director + manager + the verification harness)**, not the 1 file the plan scoped. The harness references are string-based (`FindConsoleVariable`) so they would *not* break compilation if left, but they would become dangling references to deleted CVars. Phase 1's "rich-basic routing remnants" are also entangled here — the dead `RoutedRich_FallbackBranch` ternary arm (`T66EnemyDirector.cpp:1597-1598`, unreachable because lightweight basics always `continue` at `:1518/:1530`) and the now-constant `bUseLightweightRouting` plumbing/helpers.

**Codex ruling requested — choose extent:**
- **(A1)** Remove only the 5 CVar *definitions* + the manager's `ShouldUseTouchDamageOverlap` helper/caller + the 4 `RouteAttributionSummary` log fields, and **leave** the `T66PlayerController_Overlays.cpp` harness references (they no-op: `FindConsoleVariable`→null, reads→defaults). Minimal, lowest risk to the verification harness, but leaves dead plumbing.
- **(A2)** Full clean removal: also strip all CVar set-sites + command-line overrides + the `MobRoutingFlags` summary fields from `T66PlayerController_Overlays.cpp`, plus the dead route-attribution remnants. Cleanest, but edits the `enemywaveperf` instrument and several smoke modes.
- Plus confirm whether Phase 1 should also drop the now-constant `bUseLightweightRouting` helpers/plumbing or leave them as harmless constants.

---

## DIVERGENCE B (Phase 4) — GamblerToken footprint larger than plan listed

Removing the `ET66SecondaryStatType::GamblerToken` enum value forces edits to **every** `switch`/comparison referencing it or the build breaks. Plan listed 6 files; actual reference set:

- Enum def + classifier: `T66DataTypes.h:917,936`
- `T66LocalizationSubsystem.cpp:312,385,596` (3 switch cases)
- `T66ItemCardTextUtils.cpp:50,96`
- `T66RunStateSubsystem_Private.h:296` + alias `:39,254-255`
- `T66RunStateSubsystem_EconomyInventory.cpp:961`
- `T66BackendRunSummaryParser.cpp:164` (legacy backend **string** key `"GamblerToken"` → maps to `VendorToken`)
- Save fields: `T66RunSaveGame.h:378` (`ActiveGamblersTokenLevel`), `T66ProfileSaveGame.h:132` (`GamblersTokenUnlockedLevel`)
- Field read/write: `T66RunStateSubsystem_Snapshot.cpp:27,171`; `T66AchievementsSubsystem.cpp:313,1213,1230,1240`
- `Item_GamblersToken` alias: `T66GameInstance.cpp:34,46,57`; `T66RunStateSubsystem_Private.h:39,254-255`; `T66HUDPresentationController.cpp:901`; `T66PlayerController_Combat.cpp:127`; `T66PlayerController_Overlays.cpp:371`

All legacy branches alias to canonical `VendorToken`, so removal is behavior-neutral (per the no-migration decision). **Assessment:** this is the mandatory footprint of the approved deletion, not new scope — but it is ~13 files vs. the 6 listed, so flagging for awareness. Will proceed on Codex confirmation.

---

## DIVERGENCE C (Phase 6) — STOP: B.13 is not a registered worktree

- `C:\UE\T66_B13_Worktree` **exists** (exact path match).
- **It is NOT in `git worktree list`.** Registered worktrees: `C:/UE/T66` (main), `C:/UE/T66_B11B12_Worktree`, and four `C:/Users/DoPra/.cursor/worktrees/T66/*`. B13 is absent from worktree metadata (`worktree list --porcelain` → no B13 entry).
- It has **no `.git`** at all (no dir, no gitlink file) — `git status` inside it: `fatal: not a git repository`. It is a **plain orphaned full-tree copy** of the project (contains `Source`, `Content`, `Binaries`, `DerivedDataCache`, `Intermediate`, `Saved`, `Reports`, `.uproject`, etc.).
- Preserved B.13 evidence **is** present in the live repo: `Reports/AgentReviews/20260529_B13_NoLand_Closeout/preserved_worktree_evidence/` (36 files: HISM/ISM proof logs + capture-results JSON).

**Why this is a stop:** the plan/R6 assumed a registered git worktree (use `git worktree remove`). It is neither a registered worktree nor a git repo, so `git worktree remove` is N/A and only a raw `Remove-Item -Recurse -Force` would apply. Because it has no git tracking, I cannot confirm via git that it holds no unique uncommitted work, and confirming via content diff would require a broad scan over Unreal binary asset folders — explicitly **not approved**. Per mandate ("report unexpected state before destructive deletion") I am holding the deletion.

**Codex ruling requested:** approve raw `Remove-Item -LiteralPath C:\UE\T66_B13_Worktree -Recurse -Force` of the orphaned non-git directory given preserved evidence is confirmed in-repo, or specify an alternate verification before deletion.

---

## Items ready to execute on approval (clean, in-scope)
- Phase 3: delete `AT66BossProjectile` + its Backrooms filter line/include.
- Phase 4: GamblerToken legacy removal across the full footprint above (no migration).
- Phase 5: remove all 5 CoreRedirects (`DefaultEngine.ini:16-21`).

## Items blocked pending ruling
- Phase 1 + 2: CVar / route-attribution removal extent (Divergence A: A1 vs A2).
- Phase 6: B.13 directory deletion (Divergence C).
