# Source Item Sprites

This folder is the source root for file-backed item sprite fallbacks.

It is declared as a Mini runtime dependency because `UT66MiniVisualSubsystem` can load item fallback PNGs from this path when a cooked icon is unavailable.
